{
  "name": "mailgun-nodemailer-transport",
  "version": "3.0.2",
  "description": "Mailgun API V3 and Nodemailer v4+. The plugin is very small, optimized and written in TypeScript",
  "private": false,
  "author": "Sergey Khomushin <xr0master@gmail.com>",
  "license": "MIT",
  "main": "build/MailgunTransport.js",
  "types": "build/MailgunTransport.d.ts",
  "keywords": [
    "mailgun",
    "nodemailer",
    "transport",
    "mailgun nodemailer transport",
    "nodemailer mailgun transport"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/xr0master/mailgun-nodemailer-transport.git"
  },
  "scripts": {
    "build:build-ts": "tsc",
    "build:clean": "rm -rf ./build",
    "build": "npm run build:clean && npm run lint && npm run build:build-ts",
    "test": "jest --coverage",
    "lint": "tsc --noEmit && eslint src"
  },
  "dependencies": {
    "form-data": "^4.0.0"
  },
  "peerDependencies": {
    "nodemailer": ">=6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.2",
    "@types/nodemailer": "^6.4.8",
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "@typescript-eslint/parser": "^6.2.0",
    "eslint": "^8.43.0",
    "eslint-import-resolver-typescript": "^3.5.5",
    "eslint-plugin-import": "^2.27.5",
    "jest": "^29.5.0",
    "nodemailer": "^6.9.3",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100,
    "trailingComma": "all"
  }
}
